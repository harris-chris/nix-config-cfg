This is a flake-based configuration, so if making changes to home-manager, you can test with `nix build .#homeConfigurations.chris.activationPackage`
If you want to apply these changes, then `./result/activate`.
